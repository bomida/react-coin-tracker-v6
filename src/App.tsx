import Router from './Router';
import GlobalStyle from './GlobalStyle';

function App() {
    // const isDark = useRecoilValue(isDarkAtom);

    return (
        <>
            <GlobalStyle />
            <Router />
        </>
    );
}

export default App;
